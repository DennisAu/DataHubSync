# DataHubSync - 项目待办清单 (v2.0 极简设计)

> **项目路径**：`/opt/projects/DataHubSync`  
> **设计版本**：v2.0 - 极简设计  
> **更新日期**：2025-02-04

---

## ✅ 已完成

| 任务 | 状态 | 备注 |
|------|------|------|
| Cloudflare Tunnel 创建 | ✅ | UUID: `829bd79d-7e4f-4177-8778-2985f3d4120c` |
| DNS 配置 | ✅ | `data.quantrade.fun` 指向 Tunnel |
| cloudflared 服务化 | ✅ | Windows 服务 `Cloudflared` 已运行 |
| 系统设计文档 v2.0 | ✅ | 极简设计，预打包方案 |

---

## 📋 待办任务 (按优先级)

### Phase 1: hub 端核心功能

#### 任务1.1: 新鲜度检测器
**目标**: 实现数据表新鲜度检测，85%阈值，10分钟轮询，30秒防抖

**验收标准**:
- [ ] 读取 `period_offset.csv` 获取交易日历
- [ ] 扫描数据目录，计算新鲜文件比例
- [ ] 超过85%阈值时，启动30秒防抖观察
- [ ] 30秒内新鲜度稳定（不再变化）→ 触发打包
- [ ] 30秒内新鲜度仍在变化 → 跳过，等下一轮
- [ ] 每10分钟执行一次检查

**关键逻辑**:
```python
if fresh_ratio >= 0.85:
    # 启动30秒防抖
    time.sleep(30)
    fresh_ratio_2 = recalculate()
    
    if abs(fresh_ratio_2 - fresh_ratio) < 0.01:  # 基本稳定
        trigger_packaging()
    else:
        # 仍在变化，跳过
        pass
```

#### 任务1.2: 异步打包器
**目标**: 后台打包数据目录为 zip

**验收标准**:
- [ ] 异步执行，不阻塞HTTP服务
- [ ] 打包到 `.cache/{dataset}_{timestamp}.zip`
- [ ] 完成后更新 `last_updated` 状态
- [ ] 只保留最近2个版本的 zip

#### 任务1.3: HTTP 服务器
**目标**: 提供两个端点

**验收标准**:
- [ ] `GET /api/datasets` - 返回数据表列表和 last_updated
- [ ] `GET /package/{dataset}.zip` - 返回 zip 文件流
- [ ] 支持 Range 请求（断点续传）
- [ ] 日志记录

#### 任务1.4: 配置管理
**目标**: 配置文件和目录结构

**验收标准**:
- [ ] `config.yaml` - 端口、数据目录、阈值等
- [ ] `datasets_state.json` - 各数据表 last_updated 状态
- [ ] 目录结构:
  ```
  C:\DataHubSync\
  ├── config.yaml
  ├── datasets_state.json
  ├── server.py
  ├── src\           # 源码目录
  ├── .cache\        # zip 缓存
  └── logs\          # 日志
  ```

#### 任务1.5: Windows 服务化
**目标**: hub电脑开机自启

**验收标准**:
- [ ] 使用 nssm 包装为 Windows 服务
- [ ] 开机自动启动
- [ ] 崩溃自动重启
- [ ] 日志轮转

---

### Phase 2: 客户端同步脚本

#### 任务2.1: 同步核心逻辑
**目标**: Python 同步脚本

**验收标准**:
- [ ] 请求 `/api/datasets` 获取远程状态
- [ ] 对比本地 `last_updated` 时间戳
- [ ] 如果远程更新，下载 zip
- [ ] 解压覆盖本地数据
- [ ] 更新本地时间戳
- [ ] 支持 curl -C 断点续传

#### 任务2.2: 客户端配置
**目标**: 配置文件

**验收标准**:
- [ ] `config.yaml` - hub URL、本地数据目录等
- [ ] `.last_sync.json` - 各数据表同步时间戳

#### 任务2.3: 部署和定时任务
**目标**: Linux 客户端部署

**验收标准**:
- [ ] 部署到 `/opt/datahubsync/`
- [ ] `sync.sh` 执行脚本
- [ ] crontab: `15 8 * * *` (每日8:15)
- [ ] 日志输出到 `logs/sync.log`

---

### Phase 3: 测试验证

#### 任务3.1: hub 端测试
- [ ] 新鲜度检测正确（85%阈值）
- [ ] 防抖机制有效（30秒观察）
- [ ] 定时检查正常（10分钟间隔）
- [ ] HTTP 端点可用
- [ ] zip 下载正常

#### 任务3.2: 客户端测试
- [ ] 首次同步（空目录）
- [ ] 增量同步（已是最新）
- [ ] 更新同步（有新版本）
- [ ] 断点续传（中断后恢复）

#### 任务3.3: 集成测试
- [ ] 完整流程：数据更新 → 新鲜度检测 → 打包 → 客户端同步
- [ ] 6台客户端并发下载
- [ ] 8:15 前完成同步

---

### Phase 4: 多客户端部署

**目标**: 部署到其他5台客户端

**服务器列表**:
- [ ] 客户端1: 局域网
- [ ] 客户端2: 局域网
- [ ] 客户端3: 北京机房
- [ ] 客户端4: 日本机房
- [ ] 客户端5: 美国机房

**每台执行**:
1. 复制同步脚本
2. 修改 `config.yaml` 本地目录
3. 配置 crontab
4. 测试同步

---

## 📂 目录结构

### hub 电脑 (Windows)
```
C:\DataHubSync\
├── config.yaml                    # 配置
├── datasets_state.json            # 数据表状态
├── period_offset.csv              # 交易日历（用户提供）
├── server.py                      # 入口
├── src\
│   ├── __init__.py
│   ├── freshness_checker.py       # 新鲜度检测
│   ├── packager.py                # 异步打包
│   ├── http_server.py             # HTTP服务
│   └── scheduler.py               # 定时调度
├── .cache\                        # zip 缓存
│   ├── stock-trading-data-pro_20250204_201500.zip
│   └── stock-fin-data-xbx_20250204_070500.zip
└── logs\                          # 日志
    ├── server.log
    └── freshness.log
```

### 客户端 (Linux)
```
/opt/datahubsync/
├── config.yaml                    # 配置
├── .last_sync.json                # 本地同步时间戳
├── sync.py                        # 同步脚本
├── sync.sh                        # 执行脚本
└── logs/
    └── sync.log

/data/                             # 数据目录
├── stock-trading-data-pro/
├── stock-fin-data-xbx/
└── ...
```

---

## 🔧 关键配置

### hub 端 config.yaml
```yaml
server:
  port: 8080
  data_root: "F:\\xbx_datas"
  cache_dir: "F:\\xbx_datas\\.cache"

datasets:
  - name: "stock-trading-data-pro"
    path: "stock-trading-data-pro"
    freshness_threshold: 0.85
  - name: "stock-fin-data-xbx"
    path: "stock-fin-data-xbx"
    freshness_threshold: 0.85

calendar:
  period_offset_file: "F:\\xbx_datas\\period_offset.csv"

check:
  interval_minutes: 10
  debounce_seconds: 30

packaging:
  format: "zip"
  keep_versions: 2
```

### 客户端 config.yaml
```yaml
hub:
  url: "https://data.quantrade.fun"
  timeout: 300

datasets:
  - name: "stock-trading-data-pro"
    local_dir: "/data/stock-trading-data-pro"
  - name: "stock-fin-data-xbx"
    local_dir: "/data/stock-fin-data-xbx"

logging:
  level: INFO
  file: "logs/sync.log"
```

---

## 🎯 检查清单

### 每日运行检查
- [ ] 20:00 数据更新后，30分钟内完成打包
- [ ] 次日 8:15 前所有客户端同步完成
- [ ] 日志无错误

### 监控项
- [ ] hub 端服务运行状态
- [ ] 新鲜度检测是否正常
- [ ] zip 包生成时间
- [ ] 客户端同步时间

---

## 📚 参考文档

- [需求文档](requirements/REQUIREMENTS_CLOUDFLARE_TUNNEL.md)
- [设计文档 v2.0](requirements/SOFTWARE_DESIGN_CLOUDFLARE_TUNNEL.md)
- [项目 README](/opt/projects/DataHubSync/README.md)

---

*更新时间: 2025-02-04*
